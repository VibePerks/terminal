# Local `go build ./...` output. The runnable binary is downloaded from the GitHub
# Release (or built on first use) by the bin/ launcher - never commit build artifacts.
/vibeperks-terminal
/vibeperks-terminal.exe
